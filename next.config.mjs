/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.dummyjson.com', 'dummyjson.com', 'www.themealdb.com'],  // Add TheMealDB domain here
  },
}

export default nextConfig;
